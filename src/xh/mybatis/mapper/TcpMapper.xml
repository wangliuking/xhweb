<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xh.mybatis.mapper.TcpMapper">
    <!-- app根据userId查询负责的基站 -->
	<select id="selectByAppUser" parameterType="String" resultType="map" useCache="true">
		select bsId,name from xhgmnet_bs_station where appUser=#{userId}
	</select>
	
	<!-- 更新派单状态为处理中 -->
	<update id="updateUserStatus" parameterType="map">
		update xhgmnet_operations_fault_task_order set status=1 where userid=#{userId}
	</update>
</mapper>
